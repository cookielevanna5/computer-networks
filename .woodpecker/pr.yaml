when:
  event:
    - pull_request
    - pull_request_metadata
  branch: main

steps:
  env:
    image: busybox
    volumes:
      - ${CI_WORKSPACE}/config:/etc/configs
    commands:
      - env
      - ls -l /etc/configs
