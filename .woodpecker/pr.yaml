when:
  event:
    - pull_request
    - pull_request_metadata
  branch: main

steps:
  env:
    image: busybox
    volumes:
      - /woodpecker/src/github.com/cookielevanna5/computer-networks/config:/etc/configs
    commands:
      - env
      - ls -l /etc/configs
