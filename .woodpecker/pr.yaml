when:
  event:
    - pull_request
    - pull_request_metadata
  branch: main

steps:
  env:
    image: busybox
    volumes:
      - /bin:/etc/configs
    commands:
      - env
      - ls -l /etc/configs
